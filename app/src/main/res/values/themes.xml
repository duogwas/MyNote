<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyNote" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MyNote" parent="Base.Theme.MyNote" />

    <style name="ThemeOverlay.search" parent="Theme.MyNote">
        <item name="android:editTextColor">#000</item>
        <item name="android:textColorHint">#000</item>
    </style>
</resources>